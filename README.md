📝 Text Area with Real-Time Character Counter

A simple web project that implements a text area with a real-time character counter using HTML, CSS, and JavaScript.
The counter updates dynamically as the user types and restricts input when the maximum character limit is reached.

🚀 Features

✍️ Text area for user input

🔢 Real-time character counter

📊 Displays typed and remaining characters (e.g., 150 / 200)

🚫 Prevents typing beyond the maximum limit

⚠️ Warning message when the character limit is reached

🎨 Clean and centered UI design

✨ Optional shake animation for better user feedback

🛠️ Technologies Used

HTML5 – Page structure

CSS3 – Styling and animations

JavaScript – Real-time character counting and validation

📂 Project Structure
character-counter/
│
├── index.html
├── style.css
├── script.js
└── README.md

📌 How It Works

An input event listener tracks user input in the text area.

JavaScript calculates the number of characters typed.

The counter updates in real time.

If the user exceeds the character limit, extra characters are removed and a warning is displayed.
